// Settings page entry point.
// UI will be implemented in subsequent work items.

export {};

const app = document.getElementById("app");
if (app) {
  app.textContent = "Fic Downloader — Settings";
}
